const csvAdapter = require('./csv.adapter');
const googleSheetsAdapter = require('./google-sheets.adapter');
const notionAdapter = require('./notion.adapter');

module.exports = { csvAdapter, googleSheetsAdapter, notionAdapter };
